// @arfreelance/gulp

const gulp = require("gulp");
const adds = require("require-dir")();

// Exports
// -----------------------------------------------------------------------------

module.exports = Object.assign(gulp, adds);
