{
    "name": "arfreelance",
    "version": "0.0.0",
    "private": true,
    "license": "ISC",
    "author": "Lucas Banegas <conobanegas@gmail.com>",
    "description": "Package in experimental stage. Do not use for production.",
    "homepage": "https://github.com/conobanegas/arfreelance#readme",
    "repository": "https://github.com/conobanegas/arfreelance.git",
    "bugs": "https://github.com/conobanegas/arfreelance/issues",
    "scripts": {
        "prepare": "husky install",
        "test:eslint": "eslint --color --exit-on-fatal-error --fix --no-error-on-unmatched-pattern **/*.js",
        "test:prettier": "prettier --color --ignore-unknown --loglevel error --no-error-on-unmatched-pattern --write **/*.{css,js,json,md,scss}",
        "test": "run-s test:prettier test:eslint"
    },
    "devDependencies": {
        "@arfreelance/eslint-config": "^0.1.0",
        "@arfreelance/eslint-plugin": "^0.1.0",
        "@commitlint/cli": "^17.0.3",
        "@commitlint/config-conventional": "^17.0.3",
        "commitizen": "^4.2.5",
        "cz-conventional-changelog": "^3.3.0",
        "husky": "^8.0.1",
        "lerna": "^5.3.0",
        "lint-staged": "^13.0.3",
        "npm-run-all": "^4.1.5",
        "prettier": "^2.7.1"
    },
    "config": {
        "commitizen": {
            "path": "./node_modules/cz-conventional-changelog"
        }
    },
    "commitlint": {
        "extends": [
            "@commitlint/config-conventional"
        ]
    },
    "lint-staged": {
        "*.{css,json,scss,md}": "prettier --color --loglevel error --write",
        "*.js": [
            "prettier --color --loglevel error --write",
            "eslint --color --exit-on-fatal-error --fix"
        ]
    }
}
