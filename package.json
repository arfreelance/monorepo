{
    "name": "@arfreelance/monorepo",
    "version": "0.0.0",
    "private": true,
    "license": "ISC",
    "author": "Lucas Banegas <conobanegas@gmail.com>",
    "description": "Package in experimental stage. Do not use for production.",
    "homepage": "https://github.com/arfreelance/monorepo#readme",
    "repository": "https://github.com/arfreelance/monorepo.git",
    "bugs": "https://github.com/arfreelance/monorepo/issues",
    "scripts": {
        "prepare": "husky install",
        "test:eslint": "eslint --color --exit-on-fatal-error --fix --no-error-on-unmatched-pattern **/*.js",
        "test:prettier": "prettier --color --ignore-unknown --loglevel error --no-error-on-unmatched-pattern --write **/*.{css,js,json,md,scss}",
        "test:stylelint": "stylelint --allow-empty-input --color --fix **/*.{css,scss}",
        "test:parallel": "run-p test:eslint test:stylelint",
        "test": "run-s test:prettier test:parallel"
    },
    "devDependencies": {
        "@arfreelance/eslint-config": "^0.6.0",
        "@arfreelance/eslint-plugin": "^0.6.0",
        "@arfreelance/stylelint-config": "^0.6.0",
        "@arfreelance/stylelint-plugin": "^0.6.0",
        "@commitlint/cli": "^17.0.3",
        "@commitlint/config-conventional": "^17.0.3",
        "commitizen": "^4.2.5",
        "cz-conventional-changelog": "^3.3.0",
        "husky": "^8.0.1",
        "lerna": "^5.3.0",
        "lint-staged": "^13.0.3",
        "npm-run-all": "^4.1.5",
        "prettier": "^2.7.1",
        "stylelint": "^14.9.1"
    },
    "config": {
        "commitizen": {
            "path": "./node_modules/cz-conventional-changelog"
        }
    },
    "commitlint": {
        "extends": [
            "@commitlint/config-conventional"
        ]
    },
    "lint-staged": {
        "*.{css,scss}": [
            "prettier --color --loglevel error --write",
            "stylelint --allow-empty-input --color --fix"
        ],
        "*.js": [
            "prettier --color --loglevel error --write",
            "eslint --color --exit-on-fatal-error --fix"
        ],
        "*.{json,md}": [
            "prettier --color --loglevel error --write"
        ]
    }
}
